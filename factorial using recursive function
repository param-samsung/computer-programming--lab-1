#include <stdio.h>
int factorial(int a);
int main() {
    int a,c;
    printf("enter value: ");
    scanf("%d",&a);
    c=factorial(a);
    printf("factorial of %d is %d",a,c);
    
}
 int factorial(int a){
    if(a)
    return (a*factorial(a-1));
    else
    return 1;
}
